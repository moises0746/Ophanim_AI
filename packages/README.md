# Shared Packages

This first-party boundary currently contains placeholders only:

- `contracts/` is reserved for versioned cross-component contracts.
- `shared-types/` is reserved for shared language-neutral or generated types.

Do not put provider-specific logic here. Adding package tooling or executable contracts requires a separately authorized task.
