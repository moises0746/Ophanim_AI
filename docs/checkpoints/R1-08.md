# R1-08 Checkpoint — Hub/Node Versioned Protocol Schemas & Anti-Replay

## Status

Complete. Next authorized task: **R1-09 / Rust Device Node Core Daemon**.

## Delivered

- **Hub-Node Protocol Domain Schemas** (`ophanim/domain/protocol.py` & `identifiers.py`):
  - Defined `CURRENT_PROTOCOL_VERSION = "1.0.0"`.
  - Added domain identifier `LeaseId`.
  - Defined `ProtocolMessageType` (`ENROLLMENT_REQUEST`, `ENROLLMENT_RESPONSE`, `HEARTBEAT`, `HEARTBEAT_ACK`, `LEASE_OFFER`, `LEASE_ACCEPT`, `LEASE_REJECT`, `EXECUTION_REPORT`, `CANCELLATION_NOTICE`).
  - Implemented `ProtocolHeader`, `SystemMetrics`, and universal `HubNodeMessage` envelope.
  - Implemented typed payloads for enrollment, heartbeats, task leases, execution reports with evidence hashes, and cancellation.
  - Implemented domain freshness check `validate_freshness(max_drift_seconds)`.
- **Port** (`ophanim/ports/protocol.py`):
  - `ProtocolCodecPort`: Protocol for encoding and decoding protocol messages over JSON/WSS streams.
- **Adapters** (`ophanim/adapters/protocol.py`):
  - `JsonProtocolCodec`: Standard JSON codec verifying protocol version consistency and structure.
  - `AntiReplayTracker`: Monotonic sequence validator and freshness window validator preventing message replay or out-of-order attacks per device.
- **Tests** (`services/ophanim-core/tests/test_hub_node_protocol.py`):
  - 5 test cases verifying JSON roundtrip, schema validation, version mismatch rejection, monotonic sequence tracking, and drift window rejection.

## Validation

- `pytest`: 110 passed, 1 upstream Starlette deprecation warning.
- `ruff check`: passed across all 72 files.
- `ruff format --check`: passed across all 72 files.
- Domain architecture boundary checks: passed with 0 forbidden imports.
- `git diff --check`: passed cleanly.
