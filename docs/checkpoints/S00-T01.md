# S00-T01 Checkpoint

## Task ID

`S00-T01` — Legacy Runtime Naming Cleanup

## Status

Completed — pending review

Completed at: `2026-08-08T21:14:32+08:00`

Next authorized task: **NONE**

## Objective

Migrate the existing first-party runtime naming from NEXUVO to Ophanim without changing runtime behavior, architecture, dependencies, API shape, browser behavior, provider behavior, or vendor source.

## Scope Completed

- Inventoried first-party NEXUVO references before editing.
- Renamed the first-party service directory and Python package.
- Updated Python imports, tests, project metadata, configuration prefix, service presentation strings, setup documentation, and first-party placeholder references.
- Preserved the existing AnythingLLM, LM Studio, BrowserUseAgent, browser endpoint, browser policy, and API behavior apart from required branding/service-name strings.
- Documented a clean pre-release environment-variable namespace break.
- Validated editable installation, import resolution, tests, lint, mechanical source equivalence, terminology, and protected paths.

## Before/After Path Map

```text
services/nexuvo-core/                  -> services/ophanim-core/
services/nexuvo-core/nexuvo/           -> services/ophanim-core/ophanim/
nexuvo.* Python imports                -> ophanim.* Python imports
nexuvo-core package/service identifier -> ophanim-core
NEXUVO_* environment variables         -> OPHANIM_*
NEXUVO Core presentation               -> Ophanim Core
```

The package remains directly under `services/ophanim-core/ophanim`. Moving it under a `src/` layout is not part of S00-T01.

## Files Changed

### Renamed runtime files

- `services/nexuvo-core/pyproject.toml` -> `services/ophanim-core/pyproject.toml`
- `services/nexuvo-core/nexuvo/__init__.py` -> `services/ophanim-core/ophanim/__init__.py`
- `services/nexuvo-core/nexuvo/config.py` -> `services/ophanim-core/ophanim/config.py`
- `services/nexuvo-core/nexuvo/main.py` -> `services/ophanim-core/ophanim/main.py`
- `services/nexuvo-core/nexuvo/adapters/__init__.py` -> `services/ophanim-core/ophanim/adapters/__init__.py`
- `services/nexuvo-core/nexuvo/adapters/anythingllm.py` -> `services/ophanim-core/ophanim/adapters/anythingllm.py`
- `services/nexuvo-core/nexuvo/adapters/lmstudio.py` -> `services/ophanim-core/ophanim/adapters/lmstudio.py`
- `services/nexuvo-core/nexuvo/browser/__init__.py` -> `services/ophanim-core/ophanim/browser/__init__.py`
- `services/nexuvo-core/nexuvo/browser/agent.py` -> `services/ophanim-core/ophanim/browser/agent.py`
- `services/nexuvo-core/nexuvo/browser/models.py` -> `services/ophanim-core/ophanim/browser/models.py`
- `services/nexuvo-core/nexuvo/browser/policy.py` -> `services/ophanim-core/ophanim/browser/policy.py`
- `services/nexuvo-core/tests/test_browser_policy.py` -> `services/ophanim-core/tests/test_browser_policy.py`
- `services/nexuvo-core/tests/test_health.py` -> `services/ophanim-core/tests/test_health.py`

### Configuration and documentation

- `.env.example`
- `AGENTS.md`
- `CONTRIBUTING.md`
- `README.md`
- `STRUCTURE.md`
- `docs/architecture/api-contracts.md`
- `docs/architecture/overview.md`
- `docs/development/implementation-guide.md`
- `docs/development/local-setup.md`
- `integrations/README.md`
- `docs/checkpoints/S00-T01.md`

## Naming and Configuration Decisions

- Python distribution/project name: `ophanim-core`.
- Import package: `ophanim`.
- FastAPI title: `Ophanim Core`.
- Health response service value: `ophanim-core`.
- Configuration prefix: `OPHANIM_`.
- Setup commands use `services/ophanim-core` and `uvicorn ophanim.main:app`.
- Existing endpoint paths, request/response models, settings fields, defaults, validation, timeouts, adapter requests, and browser policy behavior remain unchanged.

## Compatibility Decision

S00-T01 uses a clean breaking rename at the pre-release stage. Ophanim Core reads only `OPHANIM_*` variables. It does not implement fallback aliases for the former namespace and does not maintain two long-term configuration namespaces.

Users with an existing local `.env` must rename variable prefixes manually. `.env.example` contains the complete new namespace and no active legacy-prefixed assignments.

## Validation and Tests Executed

- `python -m pip install -e ".[dev]"` from `services/ophanim-core`: **passed**. The initial sandboxed attempt could not reach PyPI for Hatchling; the approved network-enabled retry built and installed editable `ophanim-core==0.1.0` successfully.
- Import smoke test for `ophanim`, FastAPI app, AnythingLLM adapter, LM Studio adapter, and BrowserUseAgent: **passed**; app title was `Ophanim Core`.
- `python -m pytest`: **passed**, 4 tests.
  - browser policy: 3 passed;
  - health/API branding: 1 passed;
  - one upstream Starlette/httpx deprecation warning was reported.
- `python -m ruff check .`: **passed**.
- Configuration namespace behavior: **passed**; `OPHANIM_ENVIRONMENT` was read while a simultaneously set legacy-prefixed environment value was ignored.
- Mechanical-equivalence comparison for AnythingLLM, LM Studio, browser models/policy, BrowserUseAgent, and configuration: **passed** after only authorized naming substitutions.
- Broken `nexuvo` import search: **passed**, no matches.
- First-party active runtime/configuration/documentation search outside historical Sprint/reconciliation/checkpoint records: **passed**, no NEXUVO target terminology.
- `.env.example` assignment check: **passed**, every assignment uses `OPHANIM_`.

## Architecture Impact

Naming and filesystem identity only. The modular-monolith boundary, APIs, adapter contracts, provider behavior, browser behavior, dependencies, persistence decision, and integration order are unchanged. No S00-T02 structure scaffolding was started.

## Security Impact

No authorization, approval, credential, evidence, verification, audit, domain allowlist, or browser control changed. The clean environment-prefix break can cause existing local settings to fall back to safe defaults until `.env` files are migrated; browser automation therefore remains disabled by default rather than silently using the old prefix.

No credential values or private data were read or written.

## Migrations and Rollback

This is a source/package/configuration naming migration with no database migration. Rollback requires restoring the old paths/imports/metadata/presentation and old environment prefix together. Partial rollback would break imports or configuration discovery.

Local operators must rename existing environment variables from the former prefix to `OPHANIM_`; no automatic secret-file rewrite is performed.

## Remaining Historical NEXUVO References

NEXUVO remains only where the repository records the history and acceptance of the migration:

- `docs/sprints/SPRINT-00.md` — defines the original S00-T01 objective, path map, and acceptance criteria.
- `docs/architecture/repository-reconciliation.md` — preserves the accepted S00-T00 before-state inventory and deferred S00-T01 decision.
- `docs/checkpoints/S00-T00.md` — preserves the accepted S00-T00 historical checkpoint.
- `docs/checkpoints/S00-T01.md` — records this task's objective, before/after map, compatibility decision, and validation.

No active runtime, import, configuration assignment, setup command, target architecture, or first-party placeholder uses NEXUVO naming.

## Blockers

None for S00-T01 completion.

## Risks

- Existing untracked/local `.env` files using the former prefix require manual migration and will otherwise fall back to defaults.
- External scripts, IDE launch configurations, or deployment configuration outside this repository may still reference the old path, package, or prefix and must be updated by their owners.
- Editable installation validation used Python 3.13.14; the project declares Python 3.12+ but was not separately executed under Python 3.12 in this environment.
- The test suite emitted an upstream Starlette/httpx deprecation warning unrelated to this rename.
- A pre-existing unrelated untracked file named ` complete S00-T00 repository reconciliation` remains untouched.

## Unresolved Decisions

- Whether and when to adopt the target `src/ophanim` package layout belongs to a later explicitly authorized structure task.
- Vendor provenance, placement, and licensing remain unresolved and unchanged.
- External/local environment migration beyond the documented clean break is an operator responsibility.

## Recommended Next Task

**NONE.** Stop and wait for explicit review and authorization.
