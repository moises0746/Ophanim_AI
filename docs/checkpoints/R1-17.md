# Task Checkpoint: R1-17 (Hardening & Release Gate)

**Task ID**: R1-17
**Status**: COMPLETED
**Completed at**: 2026-08-16

**Objective**:
Close out Release 1 with an end-to-end security audit, an additive negative/security
test suite, and a deterministic release verification gate.

**Scope delivered**:
- NEW `tests/test_security_hardening.py` — 10 security-hardening tests:
  - `TestKnowledgeWorkspaceIsolation` — cross-workspace isolation (upload/list/search/
    delete invisible to a different tenant token; shared workspace id across tenants is
    expected; delete of a foreign document is 404).
  - `TestDiagnosticsApiRedaction` — DB query rows and log-search records redact a canary
    `sk-…` secret through the API boundary (no leakage into API responses).
  - `TestCombinedPolicyMatrix` — combined diagnostics + skills policy rules: unlisted
    action denied, wrong-environment denied, read action does not authorize a write,
    resource mismatch denied, allowlisted read-only actions pass, and the combined rules
    contain no state-changing actions.
- NEW `scripts/verify_release.ps1` — deterministic Release 1 gate (git whitespace,
  `ruff check`, `ruff format --check`, `pytest`, desktop `npm build`/`vitest`/Playwright
  e2e, `cargo test`, git-tracked secret-pattern scan) with PASS/FAIL/SKIP summary and
  exit code. Switches: `-CoreOnly`, `-SkipDesktop`, `-SkipNode`, `-SkipSecretScan`,
  `-RequireE2E`. The e2e step probes the configured core for CORS-readiness against the
  Playwright preview origin and SKIPs (or FAILs under `-RequireE2E`) when the runtime
  prerequisite is unmet.
- NEW `docs/development/release-verification.md` — gate usage, switch reference, e2e
  runtime provisioning steps, exit codes.
- NEW `docs/security/release-1-security-audit.md` — control-by-control mapping of
  `docs/security/security-model.md` to implementation + verification, tenant/workspace
  isolation summary, MVP Security Gate table, residual risks/deferred items, conclusion.
- Desktop fixes required by the gate (untracked WIP reconciliation, per user decision to
  restore the Assistant default and ship no fabricated data):
  - `apps/desktop/src/App.tsx` — restored `/` default route to `AssistantPage`
    (DashboardPage WIP left untracked and unwired).
  - `apps/desktop/src/app/AppShell.tsx` — removed fabricated "Pro Plan" quota card.
  - `apps/desktop/src/app/navigation.tsx` — nav label aligned to committed test + page
    eyebrow ("Models & Runtimes").
  - `apps/desktop/src/features/dashboard/*` — mechanical type fixes only (unused imports,
    `Background` opacity prop, `type`/setters) so the WIP files type-check.
  - `apps/desktop/src/test/setup.ts` — `ResizeObserver` stub for ReactFlow in jsdom.
- EDIT `docs/progress/RELEASE-1-STATUS.md` — v1.15.0, R1-17 IMPLEMENTED, Release 1
  COMPLETE, validation results refreshed (280 pytest, gate 10/10 PASS), corrected the
  Desktop UI invariant to reflect that a core-level global emergency-stop is deferred.

**Files changed**:
- NEW `services/ophanim-core/tests/test_security_hardening.py`
- NEW `scripts/verify_release.ps1`
- NEW `docs/development/release-verification.md`
- NEW `docs/security/release-1-security-audit.md`
- NEW `docs/checkpoints/R1-17.md`
- EDIT `apps/desktop/src/App.tsx`
- EDIT `apps/desktop/src/app/AppShell.tsx`
- EDIT `apps/desktop/src/app/navigation.tsx`
- EDIT `apps/desktop/src/features/dashboard/DashboardPage.tsx`
- EDIT `apps/desktop/src/features/dashboard/components/WorkflowGraph.tsx`
- EDIT `apps/desktop/src/features/dashboard/components/AnalyticsCharts.tsx`
- EDIT `apps/desktop/src/test/setup.ts`
- EDIT `docs/progress/RELEASE-1-STATUS.md`

**Architecture impact**:
Additive tests/docs/scripts only; no new runtime code or permissions. The release gate
formalizes the existing engineering checks (ruff, pytest, npm, cargo) plus a git-tracked
secret scan into one repeatable command. The hardening suite exercises existing
boundaries (policy engine, knowledge adapter workspace scoping, diagnostics redaction,
API identity) without changing their contracts.

**Security impact**:
- Negative tests prove deny-by-default policy, environment/resource scoping, read
  != write authorization, and zero secret leakage through API responses.
- The gate's secret scan flags `sk-…`, `AIza…`, `AKIA…`, and PEM private-key blocks in
  git-tracked first-party source (test canaries and vendored trees excluded).
- The audit confirms Release 1 satisfies the security model's read-only MVP controls and
  documents deferred items (global emergency-stop, approval pipeline, persistence,
  CI-integrated dependency scanning).

**Tests and results**:
- `pytest` on `services/ophanim-core`: 280 passed (incl. 10 new hardening tests).
- `ruff check` / `ruff format --check`: passed.
- Architecture guard: `ophanim/domain` has no forbidden framework/runtime imports.
- `npm run build`: 0 errors; `vitest`: 18 passed; Playwright e2e: 8 passed (against a
  provisioned CORS-ready, model-less local core at `127.0.0.1:8000`).
- `cargo test`: 4 passed in `services/ophanim-node`.
- `scripts/verify_release.ps1`: **10/10 PASS** (git diff --check, ruff check, ruff
  format --check, pytest, npm build, npm test, npm run test:e2e, cargo test, secret scan).

**Acceptance criteria verification**:
1. `tests/test_security_hardening.py` runs and passes (10 tests) — done.
2. `scripts/verify_release.ps1` exists, is deterministic, and passes end-to-end with a
   provisioned e2e runtime — done (10/10 PASS).
3. `docs/development/release-verification.md` documents the gate — done.
4. `docs/security/release-1-security-audit.md` maps the security model to implemented
   controls and verification — done.
5. Tracker updated; this checkpoint created — done.

**Migrations/rollback**: N/A — no schema changes. Rollback of the gate/suite/docs is
pure file removal; the desktop fixes are minimal reverts (App route, nav label,
AppShell card, setup stub).

**Known limitations**:
- Playwright e2e requires a provisioned core runtime (CORS-ready for origin
  `http://127.0.0.1:4173`, matching workspace/token defaults, and no configured models);
  the gate SKIPs e2e with a clear reason when that prerequisite is unmet.
- Secret scan excludes test suites (synthetic canaries) and vendored trees by design.
- No global emergency-stop endpoint exists in Core (per-task/chat cancellation only) —
  documented as a deferred item.

**Open risks/blockers**: None.

**Recommended next task (informational only)**: Release 1 review & merge of the
R1-16/R1-17 work on `task/adr-018-skill-architecture`, then Release 2 planning. Agent
stops after this checkpoint per the one-task-at-a-time rule.
