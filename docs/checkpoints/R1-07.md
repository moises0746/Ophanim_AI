# R1-07 Checkpoint — Knowledge Ingestion, Chunking & Verifiable Citations

## Status

Complete. Next authorized task: **R1-08 / Hub/Node Versioned Protocol Schemas**.

## Delivered

- **Knowledge Domain Models** (`ophanim/domain/knowledge.py` & `identifiers.py`):
  - Added domain identifiers: `DocumentId`, `ChunkId`, `CitationId`.
  - Implemented `DocumentSourceType` (`OBSIDIAN_VAULT`, `MARKDOWN_FILE`, `API_SPEC`, `RUNBOOK`, `TRANSACTION_LOG`, `MANUAL`).
  - Implemented `Document`, `DocumentChunk`, `Citation`, `KnowledgeQuery`, and `KnowledgeSearchResult`.
- **Ports & Protocols** (`ophanim/ports/knowledge.py`):
  - `KnowledgeRepositoryPort`: Protocol for persisting documents/chunks, listing workspace documents, and executing ranked citation queries with provenance tracking.
- **Adapters** (`ophanim/adapters/knowledge.py`):
  - `MarkdownDocumentIngester`: Ingests Markdown/Obsidian documentation, parses hierarchically with header paths (`### ERR_...`), computes cryptographic SHA-256 hashes, and bounds chunk character offsets.
  - `InMemoryKnowledgeAdapter`: Thread-safe knowledge repository with lexical/BM25 retrieval, workspace isolation boundaries, source type filtering, document deletion, and verifiable `Citation` excerpt generation.
- **Unit & Integration Tests** (`services/ophanim-core/tests/test_knowledge.py`):
  - 4 test cases verifying Markdown header extraction, search with verifiable citations, multi-tenant workspace isolation, source filtering, and document lifecycle.

## Validation

- `pytest`: 105 passed, 1 upstream Starlette deprecation warning.
- `ruff check`: passed across all 68 files.
- `ruff format --check`: passed across all 68 files.
- `git diff --check`: passed cleanly.
