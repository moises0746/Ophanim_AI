# R1-09 Checkpoint — Rust Device Node Core Daemon

## Status

Complete. Next authorized task: **R1-10 / Capability Scheduling, Task Leases, Cancellation & Offline Recovery**.

## Delivered

- **Rust Service Package** (`services/ophanim-node/Cargo.toml`):
  - Initialized Rust package `ophanim-node` v0.1.0 with serde, tokio, chrono, uuid, sha2, hex, thiserror.
- **Protocol Serde Models** (`services/ophanim-node/src/protocol.rs`):
  - Implemented Rust serde types matching Hub/Node protocol v1.0.0 (`HubNodeMessage`, `ProtocolHeader`, `ProtocolMessageType`, `SystemMetrics`, `HeartbeatPayload`, `LeaseOfferPayload`, `LeaseAcceptPayload`, `ExecutionReportPayload`, `CancellationNoticePayload`).
- **Identity & Config Store** (`services/ophanim-node/src/identity.rs`):
  - Thread-safe `NodeIdentityStore` with async token updating and public key fingerprint tracking.
- **Telemetry Metrics** (`services/ophanim-node/src/metrics.rs`):
  - Lightweight `MetricsCollector` providing safe system telemetry for node heartbeats.
- **Governed Capability Executor** (`services/ophanim-node/src/executor.rs`):
  - Fail-closed execution engine enforcing the strict tool allowlist (`diagnostics.ping`, `diagnostics.os_info`, `diagnostics.health_check`).
  - Strict prohibition and denial of arbitrary shell/PowerShell commands.
  - Cryptographic SHA-256 evidence receipt calculation for all executed actions.
- **Daemon Entrypoint** (`services/ophanim-node/src/main.rs` & `lib.rs`):
  - Structured daemon initialization and heartbeat generation.
- **Integration Tests** (`services/ophanim-node/tests/test_node.rs`):
  - 4 test cases validating protocol serialization, allowlisted tool execution with SHA-256 evidence hashing, security violation on unauthorized tools, and thread-safe identity configuration.

## Validation

- `cargo test`: 4 passed (0 failed, 0 warnings).
- `pytest`: 110 passed across `services/ophanim-core`.
- `ruff check` & `ruff format --check`: passed across all Python files.
- `git diff --check`: passed cleanly.
