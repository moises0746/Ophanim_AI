# Infrastructure

These first-party directories reserve deployment, local composition,
observability, and migration ownership. S00-T02 added no infrastructure
configuration or executable migration; R1-16 added the local compose stack
(`infrastructure/compose`) and Core build config
(`services/ophanim-core/Dockerfile`) plus the first-party observability module
in `services/ophanim-core/ophanim/observability/`.

