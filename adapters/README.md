# Adapters

These first-party placeholders reserve replaceable external-system boundaries. Adapters must implement Ophanim-owned typed ports and must not place provider dependencies in the domain layer.

No adapter in this directory is implemented by S00-T02.
