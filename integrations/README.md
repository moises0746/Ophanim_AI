# Integrations

First-party governed integrations belong here when they are large enough to live outside Ophanim Core. Early adapters should remain inside the modular monolith.

Every integration requires least-privilege scopes, read-only defaults, typed inputs/outputs, timeouts, policy classification, audit events, and tests using fakes.
